// functions/config/firebase-config.js
const admin = require('firebase-admin');

// Firebase Admin SDK se inicializa sin configuración explícita
// porque usa las credenciales del servicio por defecto en Firebase Functions

module.exports = admin;